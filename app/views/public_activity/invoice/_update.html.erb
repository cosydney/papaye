<li><%#= if link_to invoice_path(activity.trackable).nil? ? '#': do %>
Invoice from <%= activity.parameters[:company] %> and client <%= activity.parameters[:client_name] %> <%= activity.parameters[:client_last_name] %> has been updated
 <% #end %>
<div class="timestamp pull-right"><%= time_ago_in_words(activity.created_at)%> ago</div></li>